module.exports = (client, message) => {
  message.reply("click on the " + setup.next + " Voice Channel to connect and play " + setup.name + ".\n\n**--Other commands--**\nHow to setup the " + setup.name + ": `?setup`\nGet or change the roomsize: `?roomsize`\nGet the current number of players: `?status`")
}
