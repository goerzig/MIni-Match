module.exports = (client, message) => {
  message.reply("create a category starting with `" + setup.name + "` and inside a Voice Channel called `" + setup.next + '`.')//\n\nThe category should set "Read Text Channels and see Voice Channels" to `X` for everyone. Then, the Permissions of the `' + setup.next + '` Voice Channel should have "View Channel" set to `✔︎` for everyone.')
}
